\documentclass{article}
\usepackage{amsmath, amssymb, amsthm}
\begin{document}

\title{Accumulation Points of a Set in $(M,d)$}
\author{}
\date{}
\maketitle

\theoremstyle{definition}
\newtheorem*{definition}{Definition}
\newtheorem*{lemma}{Lemma}
\newtheorem*{proof}{Proof}

\begin{definition}[Accumulation Points of a Set in $(M,d)$]
Let $A \subset M$ and $x \in M$. A point $x$ is an accumulation point of $A$ if every open set $U$ containing $x$ contains some point of $A$ which is not $x$.
Alternatively: $\forall \varepsilon > 0, (B_{\varepsilon}(x) \setminus \{x\}) \cap A \neq \emptyset$.
\end{definition}

\noindent\textbf{Examples:}
All accumulation points of $\{\left(x, \sin\left(\frac{1}{x}\right)\right) : x > 0\} \subset \mathbb{R}^2$ are $A \cup \{(0,y) : -1 \le y \le 1\}$.

\begin{lemma}
A set is closed if and only if the accumulation points of $A$ are in $A$. Or, there are no accumulation points of $A$ in $A^c$.
\end{lemma}

\begin{proof}
$A$ closed $\iff A^c$ open $\iff \forall x \in A^c, \exists \varepsilon > 0 : B_{\varepsilon}(x) \subset A^c$
$$\iff \forall x \in A^c, \exists \varepsilon > 0 : B_{\varepsilon}(x) \cap A = \emptyset$$
$$\iff x$ is no accumulation point.
\hfill $\square$
\end{proof}

\section*{The Closure of a Set in $(M,d)$}

\begin{definition}[The Closure of a Set in $(M,d)$]
The closure of $A$ (denoted $\mathrm{cl}(A)$ or $\overline{A}$) is the intersection of all closed sets containing $A$.
\end{definition}

\begin{lemma}
$\mathrm{cl}(A)$ is closed $\bullet$ $A \subseteq \mathrm{cl}(A)$ $\bullet$ $A = \mathrm{cl}(A) \iff A$ is closed.
\end{lemma}

\begin{proof}
A intersection of closed sets is closed. Every set in the intersection contains $A$, therefore $A$ is in the intersection. If $A$ is closed, $A$ is in the intersection, so $A$ is the intersection.
\end{proof}

\noindent\textbf{Example:}
$$\mathrm{cl}\left(\{\frac{1}{n} : n \in \mathbb{N}\} \subset \mathbb{R}\right) = \left\{\frac{1}{n} : n \in \mathbb{N}\right\} \cup \{0\}$$

\begin{lemma}
Given $A \subset M$, let $L$ be the set of all accumulation points of $A$. The closure of $A$ is:
$$\mathrm{cl}(A) = A \cup L. \quad (\iff \mathrm{cl}(A)^c = A^c \cap L^c)$$
\end{lemma}

\begin{proof}

\end{proof}

\n\section*{Recommended Reading}\n\begin{itemize}\n  \item \textbf{Writing Proofs in Analysis} by Jonathan M. Kane (Match: 0.69)\n  \item \textbf{Lecture Notes} by Topologie (2018) - Andreas Cap (Match: 0.68)\n  \item \textbf{Basic Analysis I} by James K. Peterson (Match: 0.67)\n\end{itemize}\n
\end{document}